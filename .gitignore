# Saved transcripts (the root-level patterns also catch notes written outside notes/)
meeting-notes-*.md
meeting-notes-*.txt
meeting-notes-*.json
notes/

# Secrets
.env

# Your built config — the builder creates it from config.default.jsonc on first build
config.jsonc

# Python
__pycache__/
*.pyc
venv/
.venv/

# Node
node_modules/
